repo: tmsso/pixwagon
branch: main
path: design-system

## Last sync

date: 2026-08-07T19:52:08Z

### Updated in this project

- Re-pulled the whole `design-system/` tree (18 cards, foundations + components) — full re-upload, as `.design-sync/NOTES.md` expects for this shape.
- Revised `DiceFace`: combination faces (1+2, 2+2, 1+3) now stack their two blobs on separate lines with a hairline rule, instead of one crowded inline row.
- Generated the six-surface design pass from `docs/design/BRIEF.md` → `surfaces/Pixwagon — six surfaces.html`, with the brief's annotations on canvas.

## Screen map

| Surface | Built from |
| --- | --- |
| Home (`/`) | `docs/design/BRIEF.md`, `apps/web/src/design/tokens.ts`, `ui/Button.tsx`, `ui/Panel.tsx`, `game/PicturePreview.tsx` |
| Lobby (`/lobby`) | `ui/RoomCodeInput.tsx`, `game/PlayerChip.tsx`, `ui/Panel.tsx`, `game/PackCard.tsx` |
| Game (`/r/:code`) | `game/HudFrame.tsx`, `game/BoardCell.tsx`, `game/PlayerChip.tsx`, `game/RollControl.tsx` |
| Roll/offer control | `game/RollControl.tsx`, `game/DiceFace.tsx`, `game/PieceGlyph.tsx`, `docs/mechanics-correction.md` |
| Results (`/results`) | `game/PicturePreview.tsx`, `game/PlayerChip.tsx`, `ui/Button.tsx` |
| Pack picker (`/packs`) | `game/PackCard.tsx`, `game/PicturePreview.tsx`, `packages/packs/data/transportation.json` |
| Design-system cards | `design-system/**` (generated by `scripts/design-system/cards.tsx`) |

## Notes for the next handoff

- `DiceFace.tsx` needs the same two-line change in code: replace the `flex items-center` combo wrapper with a `flex flex-col items-center` stack, drop the `+` glyph, and put a 1px `--color-border` rule between the two `BlobDots` rows. The preview cards here already show the intended result.
- Colour, type, spacing values used in the surfaces are the current provisional token values, unchanged. No tokens were renamed or added.
