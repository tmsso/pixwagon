# Copy to apps/web/.env.local to override. Unset, `pnpm dev` uses the local
# `pnpm dev:server` worker and a production build uses the live worker
# (apps/web/src/net/roomOrigin.ts).
# VITE_ROOM_ORIGIN=http://localhost:8787
